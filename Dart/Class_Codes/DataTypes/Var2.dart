
//VAR CAN BE OF ANY DATA TYPE But, the data type of value at initialization stays as same for entire code. The data type cannot be changed once it initialize with the value of some specific data type.
 
void main(){
	var x="Sakshi";
	print(x);
	//x=20.5;       // Error: A value of type 'double' can't be assigned to a variable of type 'String'.
	//print(x);
	//x=true;       // Error: A value of type 'bool' can't be assigned to a variable of type 'String'.
	//print(x);
}
